/bin/
/.tmp/
node_modules/
*.out
coverage.out
coverage/
.DS_Store
.swarm-worktrees/
# compiled Go binaries (build artifacts — `make build`/`go build` output)
cli/swarm/swarm
cli/supervisor/supervisor
cli/crm-gen/crm-gen
cmd/server/server
cmd/crm-mcp/crm-mcp
.env

# swarm supervisor control sentinels
.swarm-STOP
.swarm-DONE

# Generated by `bin/swarm backlog build` (pre-flight rebuilds it; reality.json is the committed input)
/swarm/superpowers/backlog/cooked.json

# UI visual-review capture artifacts (regenerated by `make ui-shots`)
/frontend/storybook-static/
/frontend/.shots/

# FE build outputs (poc never ran fe-build; skeleton does)
frontend/dist/
frontend/storybook-static/
