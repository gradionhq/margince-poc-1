# GH-206 — Manual/Live UAT: README + Epic 01 manual guide

Documentation-only ticket — no handler/business logic ships in this branch. Every step below is
a doc-inspection check, not a live HTTP round-trip; the stack does not need to be running.

1. **[auto]** Run `test -f docs/manual-guide/epic01/README.md && echo present`.
   Expected: prints `present`.
2. **[auto]** Run `grep -c "^## Setup" docs/manual-guide/epic01/README.md`.
   Expected: `1` — exactly one shared Setup section (no per-ticket setup repeated elsewhere).
3. **[auto]** Run `grep -c "^## Part 1 — People" docs/manual-guide/epic01/README.md`, `grep -c
   "^## Part 2 — Pipelines" docs/manual-guide/epic01/README.md`, `grep -c "^## Part 3 —
   Archive/Restore" docs/manual-guide/epic01/README.md`, and `grep -c "^## Automated
   counterpart" docs/manual-guide/epic01/README.md`.
   Expected: each is `1` — all four sections present, in that order.
4. **[auto]** Run `grep -n "make check\|test-contracts\|test-integration\|test-liveuat"
   docs/manual-guide/epic01/README.md | wc -l`.
   Expected: a non-zero count — the automated counterpart names all four real Makefile targets.
5. **[auto]** Run `grep -c "workspace/manual-test/t" docs/manual-guide/epic01/README.md`.
   Expected: a non-zero count — the guide links back to at least one per-ticket source guide.
6. **[auto]** Run `grep -n "What to expect once you're in" -A2 README.md`.
   Expected: no longer says "the ones without a real feature yet" is the *only* description —
   People/Companies/Deals/360 views are named as real, shipped screens.
7. **[auto]** Run `grep -n "There is no .docs/. content" README.md`.
   Expected: no match — the stale sentence is gone.
8. **[auto]** Run `grep -c "docs/manual-guide/epic01/README.md" README.md`.
   Expected: at least `1` — README links to the new guide.
9. **[auto]** Run `git diff README.md` (against `main`) and inspect it.
   Expected: only the "What to expect once you're in" and "Docs" sections changed; "Boot it",
   "Log in", "Verify it boots without clicking anything", and "The gate suite" show no diff.
10. **[auto]** Run `make check-doc-style && make check-craft-doc`.
    Expected: both exit 0.
11. **[auto]** Run `make check`.
    Expected: exits 0 — all 19 gates pass with no code/contract/test files touched by this
    branch.
