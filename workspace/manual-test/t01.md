# T01 — Manual/Live UAT: people/org read-surface extensions (PO-EXT-1/2/3)

Contract-only ticket — no handler/business logic ships in this branch (spec Out-of-scope),
so most steps are contract/gate inspection rather than live HTTP round-trips. Steps that
would require a real `listPeople`/`getPerson`/`getOrganization` handler (not yet
implemented in this repo) are marked `[manual]` with the reasoning, per Pre-implementation
finding #2 of `workspace/plans/2026-07-04-t01.md`.

Prereqs: `make infra-up && make migrate-up && make seed-reset && make run` (or the
project's standard live-stack bootstrap) — only needed for step 7.

1. **[auto]** Run `make gen-types` then `git status --short`.
   Expected: exits 0; diff (if any, from a prior dirty state) touches only
   `backend/api/crm.yaml`, `backend/internal/contracts/types/crm_gen.go`,
   `frontend/src/lib/api-client/generated/crm.d.ts` — clean regeneration, no drift.

2. **[auto]** Run `grep -n "strength:" backend/api/crm.yaml | head -5` and inspect the
   `Person` schema (`grep -n -A8 "^    Person:" backend/api/crm.yaml`).
   Expected: `strength` object present with `score`/`bucket`/`recency`/`frequency`/
   `reciprocity`, matching the pinned compact shape (score int 0-100, bucket enum
   weak/moderate/strong).

3. **[manual]** Confirm `strength` is an allowed `listPeople` sort value.
   Expected: `docs/architecture/data-model.md`'s `DM-VOCAB-1` row lists `strength` among
   people's allowed sort fields, and `backend/api/crm.yaml`'s `Sort` parameter
   (`grep -n -A5 "^    Sort:" backend/api/crm.yaml`) is still a plain `{type: string}` — no
   enum was added. (A live `listPeople?sort=strength` round-trip isn't possible yet: no
   `listPeople` Go handler exists in this repo — contract-only ticket, confirmed via
   `grep -rl listPeople backend/internal --include='*.go'` returning nothing besides
   `crm_gen.go`.)

4. **[auto]** Run `grep -n "getPersonStrengthBreakdown\|PersonStrengthBreakdown:" backend/api/crm.yaml`.
   Expected: operation `getPersonStrengthBreakdown` (`GET /people/{id}/strength-breakdown`)
   and schema `PersonStrengthBreakdown` both present; the schema's `contributing_activities`
   property is an array of `ActivityRef` (id/kind/subject/occurred_at).

5. **[auto]** Run `grep -n -A4 "relationships:" backend/api/crm.yaml | grep -B1 -A3 "PO-EXT-3"`.
   Expected: both `Person` and `Organization` carry `relationships`/`deals`/`activities`
   array properties, each documented as populated only on the single-record `getPerson`/
   `getOrganization` read.

6. **[auto]** Run:
   ```bash
   oasdiff breaking origin/main:backend/api/crm.yaml backend/api/crm.yaml --fail-on ERR
   ```
   (the exact command `scripts/check-contract-breaking.sh` runs).
   Expected: exits 0 — confirms every PO-EXT-1/2/3 change landed additive-only.

7. **[auto]** Run `make check`.
   Expected: exits 0; `GATE-CORE-1` (`gen-types-check`) passes.

8. **[auto]** Run `make test-contracts`.
   Expected: all TS contract-compliance tests `PASS`, including the three new `describe`
   blocks for `Person.strength`, `PersonStrengthBreakdown`, and the person/org 360 reads.
