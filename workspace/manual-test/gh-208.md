# GH-208 — Manual/Live UAT: deal archive 404 fix

Boot: `make infra-up && make migrate-up && make seed-reset && make run`, then in a second
terminal `make fe-dev`. Log in as `admin@example.com` / `changeme` (or any seeded role).

1. **[live]** Open a seeded deal's detail page (e.g.
   `/deals/00000000-0000-0000-0042-000000000002`, the exact URL from the reported repro).
   Open devtools' Network tab, click **"Archive…"**. Expected: the request is
   `DELETE /api/deals/<id>`, response status **200** (not 404), body is the archived deal JSON
   (has `archived_at` set) — no plain-text "404 page not found" body.

2. **[live]** After archiving, the UI reflects the archived state (e.g. the deal no longer
   appears in the active pipeline board / list view, or the detail page shows an
   "archived"/restore affordance — whatever the existing Organization archive UX shows,
   since Deal mirrors it).

3. **[auto]** `cd backend && go test ./internal/modules/directory/... ./internal/modules/directory/transport/...`.
   Expected: all green, including the new archive unit + integration tests (happy path, 404 for
   a non-existent deal, workspace-scoping, archived-excluded-from-list, event/audit-write).

4. **[auto]** `make test-integration`. Expected: 0 skips, all green.

5. **[auto]** `make check-q`. Expected: exits 0 (includes `audit-coverage`/`audit-coherence`,
   `arch-lint`, `rls-store-path`, `gen-types-check`, `go test`, and every other merge-gate
   check).

6. **[auto]** `curl -i -X DELETE http://localhost:8080/deals/00000000-0000-0000-0000-000000000099 -H "X-Workspace-ID: 00000000-0000-0000-0000-000000000001" -H "X-User-ID: 00000000-0000-0010-000000000001"`.
   Expected: HTTP 404 with an `application/problem+json` body (`{"code":"not_found",...}`),
   never Go's plain-text `http.NotFound` body — confirms AC2 (the not-found case is a proper app
   error, not a regression of the "unhandled method" bug). *(Note: both X-Workspace-ID and X-User-ID dev headers are required; either alone hits an unrelated pre-existing 500 in the dev-auth middleware.)*
