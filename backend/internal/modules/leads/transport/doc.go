// Package transport is the leads module HTTP transport layer.
// No handler is wired yet — routes are defined in crm.yaml but not registered
// in cmd/api/routes.go (confirmed: /leads routes are not wired).
// See AC-E1: the transport/ directory must exist even if not yet populated.
package transport
